apiVersion: v1
kind: Service
metadata:
  name: moneyhero-backend-service
  namespace: moneyhero
spec:
  type: NodePort
  selector:
    app: moneyhero-backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30082